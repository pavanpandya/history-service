spring:

  datasource:
    url: jdbc:postgresql://localhost:4008/history
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always

  rabbitmq:
    port: 4006

server:
  port: 8085